export enum POST_CONSTANTS {
    POSTBASEURL = "https:/Jsonplaceholder.typicode.com/"
}

export enum POST_CRUD {
    addPosts = "/Posts",
    getPosts = "/Posts",
    deletePost = "/Posts",
    putPost = "/Posts"
}
