export enum CompletedStatus {
    undone = "undone",
    inprogress = "inprogress",
    done = "done"
}

export enum Statuses {
    idle = "idle",
    loading = "loading",
    success = "success",
    error = "error"
}
